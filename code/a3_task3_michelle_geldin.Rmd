---
title: "Assignment 3 Task 3 Michelle Geldin"
author: "Michelle Geldin"
date: "10/27/2021"
output:  
  html_document: 
    theme: yeti
    toc: yes
    code_folding: hide
---
## Overview

### Data Citation

## Setup

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
library(tidyverse)
library(here)
library(janitor)
library(kableExtra)
```

## Read in the data

```{r}
urchins_data <- read_csv(here("data", "urchins.csv" ))
```

## Section 1

### Data Wrangling

```{r}
urchins_data_clean <- urchins_data %>%
  clean_names()
```

### Data Visualization

```{r}
ggplot(data = urchins_data_clean, aes(x = size)) +
  geom_histogram(fill = "red", # How do you make one red and one purple?
                 bins = 10) +
  facet_wrap(~ common_name)
```

Figure caption:


### Descriptive Statistics Table

Table Caption:

```{r}
urchins_stats <- urchins_data_clean %>%
  group_by(common_name) %>%
  summarize(mean_size = round(mean(size), 2),
            sd_size = round(sd(size), 2), # How do you incorporate kableExtra?
            median_size = round(median(size), 2),
            sample_size = n())

urchins_stats %>%
  kable(col.names = c("Common Name",
                      "Mean Diameter (cm)",
                      "Standard Deviation",
                      "Median Diameter (cm)", 
                      "Sample Size (n)")) %>%
  kable_styling(full_width = FALSE)

urchins_stats
```

### Description of Takeaways


## Section 2

### Data Visualization

```{r}
purple_urchins <- urchins_data_clean %>%
  filter(common_name == "Purple Urchin") %>%
  
  
  
```

